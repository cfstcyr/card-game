name: Client

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          cd packages/client_v2
          npm ci
          npm build
        shell: bash